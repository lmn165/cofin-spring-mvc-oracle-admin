<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="shop.cofin.oracle.mappers.PublisherMapper">
	<select id="findAll" resultType="shop.cofin.oracle.publisher.domain.PublisherDto">
		select
		    pub_id pubId,
		    pub_name pubName,
            mgr_name mgrName,
            phone
		from publishers
	</select>
	<select id="findByPubId" resultType="shop.cofin.oracle.publisher.domain.PublisherDto" parameterType="int">
		select
		    pub_id pubId,
		    pub_name pubName,
            mgr_name mgrName,
            phone
		from publishers
        where pub_id = ${pubId}
	</select>
	<select id="findByPubName" resultType="shop.cofin.oracle.publisher.domain.PublisherDto" parameterType="string">
		select
		    pub_id pubId,
		    pub_name pubName,
            mgr_name mgrName,
            phone
		from publishers
        where pub_name like '%'||'${value}'||'%'
	</select>
	<select id="findByMgrName" resultType="shop.cofin.oracle.publisher.domain.PublisherDto" parameterType="string">
		select
		    pub_id pubId,
		    pub_name pubName,
            mgr_name mgrName,
            phone
		from publishers
        where mgr_name like '${value}'||'%'
	</select>
	<select id="findByPhone" resultType="shop.cofin.oracle.publisher.domain.PublisherDto" parameterType="string">
		select
		    pub_id pubId,
		    pub_name pubName,
            mgr_name mgrName,
            phone
		from publishers
        where phone like '010-'||'${value}'||'%'
	</select>
</mapper>