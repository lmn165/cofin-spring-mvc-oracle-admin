<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="shop.cofin.oracle.mappers.CustomerMapper">
	<select id="findAll" resultType="shop.cofin.oracle.customer.domain.CustomerDto">
		select 
    		cust_id custId,
    		cust_name custName,
    		address,
    		phone
  		from customers
	</select>
	<select id="findByCustId" resultType="shop.cofin.oracle.customer.domain.CustomerDto" parameterType="int">
		select 
    		cust_id custId,
    		cust_name custName,
    		address,
    		phone
  		from customers
        where cust_id like ${custId}
	</select>
	<select id="findByCustName" resultType="shop.cofin.oracle.customer.domain.CustomerDto" parameterType="string">
		select 
    		cust_id custId,
    		cust_name custName,
    		address,
    		phone
  		from customers
        where cust_name like '${value}'
	</select>
	<select id="findByAddress" resultType="shop.cofin.oracle.customer.domain.CustomerDto" parameterType="string">
		select 
    		cust_id custId,
    		cust_name custName,
    		address,
    		phone
  		from customers
        where address like '%'||'${value}'||'%'
	</select>
	<select id="findByPhone" resultType="shop.cofin.oracle.customer.domain.CustomerDto" parameterType="string">
		select 
    		cust_id custId,
    		cust_name custName,
    		address,
    		phone
  		from customers
        where phone like '%'||'${value}'
	</select>
</mapper>